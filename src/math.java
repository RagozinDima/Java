/**
 * Created by Nataly on 04.12.2016.
 */
public class math {
    public static void main(String[] args) {
        int i = (int)Math.pow(2,5);
    }

    // min + math.random * (max-min)) -> int
    int random = (int) (20+Math.random ()* (200-20));
}
